import styled from 'styled-components';

export const CatsMainStyles = styled.main`
  padding: 2rem 0;
  flex-grow: 1;

  .title {
    margin: 1em 0 2em;
    text-align: center;
  }

  .first-cat {
    display: block;
    margin: 3rem auto;
    opacity: 0.675;
    max-width: 30rem;
  }
`;
